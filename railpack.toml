[phases.setup]
aptPkgs = ["libgl1-mesa-glx", "libglib2.0-0", "libglib2.0-dev"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[phases.run]
cmd = "gunicorn main:app --bind 0.0.0.0:$PORT --timeout 300"

[variables]
OPENCV_IO_ENABLE_OPENEXR = "1"