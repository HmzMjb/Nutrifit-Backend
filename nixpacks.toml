[phases.setup]
nixPkgs = ["libGL", "libglib2.0"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip uninstall -y opencv-contrib-python opencv-python || true",
  "pip install opencv-python-headless==4.11.0.86 --force-reinstall"
]